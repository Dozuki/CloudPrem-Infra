apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  name: featurerequests.infra.dozuki.com
spec:
  group: infra.dozuki.com
  names:
    kind: FeatureRequest
    listKind: FeatureRequestList
    plural: featurerequests
    singular: featurerequest
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: FeatureRequest is the Schema for the featurerequests API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: FeatureRequestSpec defines the desired state of FeatureRequest
              properties:
                flag:
                  description: Flag is a map of the feature flag we will manage in the
                    site_config and plan_config tables.
                  properties:
                    disabled_value:
                      description: DisabledValue is the value we set in the site_config
                        and plan_config tables when the Feature is deleted.
                      type: string
                    enabled_value:
                      description: EnabledValue is the value we set in the site_config
                        and plan_config tables when the Feature is created.
                      type: string
                    name:
                      description: Name is the feature flag itself
                      type: string
                  required:
                    - disabled_value
                    - enabled_value
                    - name
                  type: object
              required:
                - flag
              type: object
            status:
              description: FeatureRequestStatus defines the observed state of FeatureRequest
              type: object
          type: object
      served: true
      storage: true
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  name: features.infra.dozuki.com
spec:
  group: infra.dozuki.com
  names:
    kind: Feature
    listKind: FeatureList
    plural: features
    singular: feature
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: Feature is the Schema for the features API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: FeatureSpec defines the desired state of Feature
              properties:
                flag:
                  description: Flag is a map of the feature flag we will manage in the
                    site_config and plan_config tables.
                  properties:
                    disabled_value:
                      description: DisabledValue is the value we set in the site_config
                        and plan_config tables when the Feature is deleted.
                      type: string
                    enabled_value:
                      description: EnabledValue is the value we set in the site_config
                        and plan_config tables when the Feature is created.
                      type: string
                    name:
                      description: Name is the feature flag itself
                      type: string
                  required:
                    - disabled_value
                    - enabled_value
                    - name
                  type: object
              required:
                - flag
              type: object
            status:
              description: FeatureStatus defines the observed state of Feature
              type: object
          type: object
      served: true
      storage: true
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  name: subsites.infra.dozuki.com
spec:
  group: infra.dozuki.com
  names:
    kind: Subsite
    listKind: SubsiteList
    plural: subsites
    singular: subsite
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: Subsite is the Schema for the subsites API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: SubsiteSpec defines the desired state of Subsite
              properties:
                domain:
                  description: Domain is a valid URL for the Subsite
                  type: string
                sitename:
                  description: Sitename is an alphanumeric identifier for the Subsite
                  type: string
              required:
                - domain
                - sitename
              type: object
            status:
              description: SubsiteStatus defines the observed state of Subsite
              properties:
                setupComplete:
                  description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file SetupComplete indicates whether the setup for the Subsite
                  has been completed'
                  type: boolean
              type: object
          type: object
      served: true
      storage: true