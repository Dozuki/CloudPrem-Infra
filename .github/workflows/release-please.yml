name: release-please

# Conventional-commits release automation. On pushes to master, release-please
# maintains a "release PR" that bumps .release-please-manifest.json and prepends
# CHANGELOG.md from feat:/fix:/etc. commits since the last release. Merging that
# PR creates the v<version> tag + a GitHub Release with the generated changelog.
#
# Replaces the old hand-written release.yml. Version is computed from commit
# types: a `feat:` -> minor bump, a `fix:` -> patch, and a `feat!:` / `fix!:` or
# a `BREAKING CHANGE:` footer -> major bump.
#
# Token: must be a PAT (RELEASE_PAT), not the default GITHUB_TOKEN. PRs created
# with GITHUB_TOKEN never trigger workflow runs (recursion guard), which leaves
# the release PR's required checks (Terraform matrix + harness/upgrade-tests)
# stuck at "Expected" forever and the release unmergeable. Same setup as the
# helm repo. Falls back to GITHUB_TOKEN only so the job does not hard-fail if
# the secret is missing; in that state release PRs need their workflow runs
# approved by hand.

on:
  push:
    branches:
      - master

# Serialize release-please: a burst of merges to master otherwise fires overlapping
# runs that race. One pass can rebuild the release PR from pre-merge state just as
# another pass merges the release, leaving an empty duplicate release PR (this is what
# produced the phantom second "release 7.19.0" PR). Queue rather than cancel so every
# run finishes and reconciles against the latest state - the tag/release step at the
# end must never be cut off mid-flight.
concurrency:
  group: release-please-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5
        with:
          token: ${{ secrets.RELEASE_PAT != '' && secrets.RELEASE_PAT || secrets.GITHUB_TOKEN }}
          # config-file + manifest-file use the default names
          # (release-please-config.json / .release-please-manifest.json).
